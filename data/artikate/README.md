# Artikate private dataset (not included)

Place the provided annotated images here:

```
data/artikate/
  images/train/
  images/val/
  labels/train/
  labels/val/
  video/heldout.mp4
```

Then update `configs/data.yaml` (see `configs/data_artikate.yaml.example`).
